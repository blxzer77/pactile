# Campaign UI MIX — removed

This is **not** a living contract.

`cstl campaign` (status / mcp / canvas) and the `trellis-campaign` MCP were removed from cursor-trellis because they duplicated IDE Agent / Task and BYOK. Do not treat this page as an implementation guide.

History lives in archived Trellis tasks (`08-01-cursor-campaign-ui`, `08-03-campaign-canvas`, and related). If you later want a new observation surface, design from current needs in a **new Task** — do not revive the old MIX/RPC protocol from this file.

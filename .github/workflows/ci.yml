name: CI

on:
  push:
    branches: [main, develop, "feat/**", "fix/**", "chore/**"]
  pull_request:
    branches: [main, develop]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pr-policy:
    if: ${{ github.event_name == 'pull_request' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
      - name: Verify PR target and beta evidence for a release PR
        run: node packages/cli/scripts/release-pr-check.js
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  test:
    name: test (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      # Default fetch-depth is 1. Tests must not `git show` historical SHAs or
      # retired blobs — rewritten history and shallow clones will fail red.
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6
        with:
          version: 10.32.1

      - uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify strict Pactile brand surface
        run: node packages/cli/scripts/check-pactile-brand-surface.js --release

      - name: Build
        run: pnpm build

      - name: Typecheck
        run: pnpm typecheck

      - name: Lint
        run: pnpm lint

      - name: Test
        run: pnpm test

      - name: Documentation smoke
        run: pnpm docs:smoke

      - name: Verify package file contract
        run: pnpm --filter @blxzer/pactile check:pack-files

      - name: Verify release tarball contents
        run: pnpm --filter @blxzer/pactile check:release-pack

      - name: Verify packed dependency and bins
        run: node packages/cli/scripts/release-preflight.js verify-packed-cli

  release-conformance:
    name: release conformance (${{ matrix.os }}, Node ${{ matrix.node }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        node: ["18.17.0", "lts/*", "current"]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6
        with:
          version: 10.32.1

      - uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build the single release package
        run: pnpm build

      - name: Verify single-package tarball and isolated Node install
        run: node packages/cli/scripts/release-conformance.js

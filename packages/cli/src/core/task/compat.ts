import type { PactileTaskPhase } from "./phase.js";
import type { PactileTaskRecord } from "./schema.js";

/** @deprecated Use PactileTaskRecord. Retained through the 0.5.x window. */
export type TrellisTaskRecord = PactileTaskRecord;

/** @deprecated Use PactileTaskPhase. Retained through the 0.5.x window. */
export type TrellisTaskPhase = PactileTaskPhase;
